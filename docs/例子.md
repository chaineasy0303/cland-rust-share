src/
├── main.rs
├── model/           # 核心数据模型层（通用+业务模型）
│   ├── mod.rs
│   ├── common/      # 通用数据模型（跨业务复用）
│   │   ├── mod.rs
│   │   ├── pagination.rs  # 分页模型（Pagination、PageQuery）
│   │   └── enums.rs       # 通用枚举（如错误码，可选）
│   ├── user.rs      # 业务模型（用户实体）
│   └── tenant.rs    # 业务模型（租户实体）
├── dto/             # 数据传输层（API Request/Response）
│   ├── mod.rs
│   ├── common/      # 通用DTO（跨接口复用）
│   │   ├── mod.rs
│   │   └── response.rs    # 基础响应（ApiResponse）
│   └── user_dto.rs  # 业务DTO（用户相关请求/响应）
└── usecase/         # 业务逻辑层
    └── user/
        └── service.rs